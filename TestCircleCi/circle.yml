machine:
  xcode:
    version: 7.3.1
dependencies:
  override:
    - pod install:
        timeout: 300
test:
  override:
    - xcodebuild test -scheme Retty -workspace TestCircleCi.xcworkspace -destination 'name=iPhone 6'
