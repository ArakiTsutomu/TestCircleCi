machine:
  xcode:
    version: 7.3
test:
  override:
    - set -o pipefail &&
      xcodebuild
        -destination 'platform=iOS Simulator,OS=9.3,name=iPhone 6'
        -sdk iphonesimulator
        -project TestCircleCi/TestCircleCi.xcodeproj
